<h2><%= @hospital.name %></h2>

<ul>
  <% @doctors.each do |doctor| %>
    <li id="doctor_<%= doctor.id %>">
      <%= doctor.name %>
      <p>Patient Count: <%=  doctor.patient_count %></p>
    </li>
  <% end %>
</ul>
